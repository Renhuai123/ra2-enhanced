; =============================================================================
; RA2 Enhanced — Phobos QoL (rulesmd-side defaults)
; Docs: https://phobos.readthedocs.io/en/latest/User-Interface.html
; Clarity & UX only — not unit buffs.
; Companion files:
;   qol/uimd-overlay.ini   → uimd.ini ToolTips / Sidebar
;   qol/ra2md-phobos.ini   → RA2MD.INI [Phobos] user toggles template
; =============================================================================

[AudioVisual]
; Building placement ghost — helps base planning (Phobos).
PlacementPreview=yes
PlacementPreview.Translucency=75
PlacementGrid.Translucency=0

; Show designator / inhibitor ranges in SW targeting mode.
ShowDesignatorRange=true
